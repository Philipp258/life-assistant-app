# 07 — Read a local file

**Status:** red
**Severity:** blocker
**Reason:** blocked by 02. The filesystem-read tool would only get
exercised after the agent decides to call it, which requires a
working model response. The agent never gets that far.
